---
tags: [标签/数据库, 标签/MySQL, 标签/学习笔记]
created: 2026-06-20
---

# 📒 数据库基础知识 · 学习笔记

---

## 一、四个核心概念（别再搞混了！）

用 **图书馆** 来类比理解这四者的关系，非常直观 👇

| 概念 | 类比 | 本质 |
|------|------|------|
| **数据库 (DB)** | 书架上的书 | 结构化数据的集合——信息本身 |
| **数据库管理系统 (DBMS)** | 图书馆管理系统 | 管理数据的大型软件，如 MySQL、Oracle、PostgreSQL |
| **数据库系统 (DBS)** | 整个正常运转的图书馆 | DB + DBMS + 硬件 + 应用 + 人，是一个更大的概念 |
| **数据库管理员 (DBA)** | 图书馆馆长 | 负责设计、监控、调优、备份恢复、安全管理 |

> 🧠 一句话记牢：**"用 MySQL 数据库"本质是用 MySQL（DBMS）管理一个或多个数据库（DB）。**

---

## 二、DBMS 四大核心功能

| 功能 | 关键词 | 说明 |
|------|--------|------|
| **数据定义** | DDL | 创建/修改/删除表、视图、索引、触发器等对象 |
| **数据操作** | DML（CRUD） | 增删改查，开发者最常用的部分 |
| **数据控制** | 并发、事务、权限 | 保证数据正确、安全、可靠 |
| **数据库维护** | 备份恢复、监控 | 保障系统长期稳定运行 |

---

## 三、DBMS 三大类型全景图

### 🔷 1. 关系型数据库（RDBMS）
- 代表：**MySQL**（开源首选）、**PostgreSQL**（功能最全）、**Oracle**（企业级）
- 特点：严格的表结构 + SQL，适合银行交易、订单系统等需要事务保证的场景

### 🟠 2. NoSQL 数据库（为极致的性能和扩展做了妥协）

| 类型 | 代表 | 特点 | 适用场景 |
|------|------|------|----------|
| **键值数据库** | Redis | 巨大 Map，Key-Value，内存操作，性能极高 | 缓存、会话存储、计数器 |
| **文档数据库** | MongoDB | 半结构化文档（JSON/BSON），结构灵活 | 用户画像、CMS、日志存储 |
| **列式数据库** | HBase、Cassandra | 按列族存储，大量行少量列读取极快 | 大数据分析、监控、推荐系统 |
| **图形数据库** | Neo4j | 节点 + 边，专门处理复杂关系 | 社交网络、知识图谱、欺诈检测 |

### 🟢 3. NewSQL 数据库
一句话公式：**分布式存储 + SQL + 事务 = 分布式关系型数据库**

核心目标：横向扩展、强一致性、高可用、标准 SQL、ACID 事务、云原生、HTAP

代表：Google F1/Spanner、阿里 OceanBase、PingCAP TiDB

---

## 四、关系理论核心概念

### 基础术语速查

| 术语 | 一句话解释 |
|------|-----------|
| **元组 (Tuple)** | 二维表中的一行记录 |
| **码 (Key)** | 能唯一标识元组的属性集合 |
| **候选码** | 能唯一标识元组的**最小**属性集合（可以有多个） |
| **主码/主键** | 从候选码中选出的一个，每表只有一个 |
| **外码/外键** | 引用另一张表主码的属性，用于建立表间关联 |
| **主属性** | 包含在**任意一个**候选码中的属性 |
| **非主属性** | 不在任何候选码中的属性 |

> 🎯 记忆技巧：候选码是最小唯一标识 → 主键是选出来的代表 → 外键是跨表的桥梁 → 主属性与候选码绑定。

---

## 五、ER 图（数据库设计的视觉语言）

三个要素：
- **实体**：矩形框，如学生、课程
- **属性**：椭圆形，如实体的字段
- **联系**：菱形，包括 1:1、1:N、M:N 三种数量关系

---

## 六、三大范式（消除数据冗余的递进武器）

| 范式 | 核心要求 | 解决的问题 |
|------|----------|-----------|
| **1NF** | 属性不可再分（字段是原子值） | 关系型数据库的最基本要求 |
| **2NF** | 1NF + 消除非主属性对码的**部分函数依赖** | 非主属性必须完全依赖于整个主键 |
| **3NF** | 2NF + 消除非主属性对码的**传递函数依赖** | 基本解决冗余、插入/修改/删除异常 |

> 💡 函数依赖速记：X → Y 表示 X 确定 Y；部分依赖是真子集也能确定；传递依赖是 X → Y → Z。

---

## 七、主键 vs 外键

| 对比维度 | 主键 (Primary Key) | 外键 (Foreign Key) |
|----------|-------------------|---------------------|
| 核心作用 | 唯一标识每行数据 | 建立表间关联 |
| 唯一性 | 必须唯一 | 可以重复 |
| 空值 | 不能为空 | 可以为 NULL |
| 数量限制 | 每表只有一个 | 每表可以有多个 |
| 完整性保障 | 实体完整性 | 引用完整性 |

---

## 八、外键的争议：用还是不用？

### 🚫 阿里的态度：强制禁止
> "不得使用外键与级联，一切外键概念必须在应用层解决。"

### ❌ 不用外键的理由
- 增加复杂度（DELETE/UPDATE 都要考虑约束，测试不方便）
- 消耗数据库资源（级联检查、阻塞风险、更新风暴）
- 对分库分表不友好（外键无法跨库生效）

### ✅ 用外键的好处
- 保证数据一致性和完整性
- 级联操作方便，减少代码量

> 🧭 **务实建议**：低并发、单体应用可以考虑外键；分布式、高并发场景老老实实在应用层处理。

---

## 九、存储过程的兴衰

| 优势 | 劣势 |
|------|------|
| 预编译，执行快 | 调试困难，缺乏成熟工具 |
| 封装复杂逻辑，简化调用 | 移植性差（不同数据库语法不同） |
| 运行稳定可靠 | 占用数据库资源，版本管理难 |

> ⚠️ 行业趋势：互联网公司普遍禁用存储过程，推荐业务逻辑放应用层。《阿里巴巴 Java 开发手册》明确禁止。

---

## 十、DROP vs DELETE vs TRUNCATE（经典面试题！）

| 对比 | DROP | TRUNCATE | DELETE |
|------|------|----------|--------|
| **表结构** | ❌ 删除 | ✅ 保留 | ✅ 保留 |
| **数据** | 全部删除 | 全部删除 | 可条件删除 |
| **自增字段** | — | 重置为 1 | 不重置 |
| **触发器** | 不触发 | 不触发 | ✅ 触发 |
| **回滚** | ❌ 不能（DDL） | ❌ 不能（DDL） | ✅ 可以（DML） |
| **日志** | — | 不产生 binlog | 产生 binlog |
| **速度** | 最快 | 次快 | 最慢 |

> 🔑 关键记忆：**DROP 删结构，TRUNCATE 清数据不记日志，DELETE 有条件能回滚。**

---

## 十一、DML vs DDL

- **DML**（Data Manipulation Language）：操作**表记录**——INSERT、UPDATE、DELETE、SELECT → 开发者日常使用
- **DDL**（Data Definition Language）：定义**数据库对象**——CREATE、ALTER、DROP → DBA 更常用
- **DQL**（Data Query Language）：SELECT 有时单独归类，因为它不破坏数据

> 🎯 一句话：DML 操作数据，DDL 操作结构。

---

## 十二、数据库设计六步法

```mermaid
flowchart LR
    A[① 需求分析] --> B[② 概念结构设计]
    B --> C[③ 逻辑结构设计]
    C --> D[④ 物理结构设计]
    D --> E[⑤ 数据库实施]
    E --> F[⑥ 运行和维护]
```

| 阶段 | 核心工作 | 关键产出物 |
|------|----------|-----------|
| ① 需求分析 | 收集数据/功能/性能/安全需求 | 需求规格说明书、数据字典 |
| ② 概念结构设计 | 识别实体、属性、联系，画 ER 图 | ER 图、概念数据模型 |
| ③ 逻辑结构设计 | ER 图转关系模型，范式化（3NF），定约束 | 表结构设计文档 |
| ④ 物理结构设计 | 选存储引擎、建索引、分区、备份策略 | 物理设计文档、索引方案 |
| ⑤ 数据库实施 | 写 DDL 建表、导入数据、测试 | 数据库脚本、测试报告 |
| ⑥ 运行和维护 | 监控、调优、备份、权限管理 | 运维报告、优化方案 |

设计原则：数据独立性、完整性、安全性、可扩展性、标准化。

---

> 📌 这篇笔记覆盖了数据库基础的所有核心知识点，适合面试前快速回顾。作者反复强调：**这部分理论是后续学 MySQL 的地基，一定要理解记忆而非死记硬背！**

---

## AI辅助思考

- **这个知识点真正重要的地方**：这篇笔记是整个数据库学习的"地基"——DB/DBMS/DBS/DBA 的区别、三大范式、ER 图、主外键概念，这些在面试中出现频率非常高（尤其是范式和主外键）。同时，外键争议（阿里禁用外键）和存储过程兴衰是面试中展现你"工程视野"的好素材。
- **它和哪些知识点有关**：这篇和 [[MySQL基础深度拆解]]、[[MySQL复习笔记]] 是"理论→实践"的关系。三大范式直接影响你做项目时的表设计能力；DML vs DDL 的区别是理解 [[MySQL复习笔记]] 中所有 SQL 操作的前提。
- **初学者容易误解什么**：① 三大范式不是"越高越好"——实际项目中为了性能经常反范式化（比如冗余字段），面试时能说出"范式化是为了减少冗余，但实际中会根据查询需求适当反范式化"就很有水平 ② "候选码"和"主码"的区别容易搞混——候选码可以有多个，主码只选一个 ③ 外键"禁用"不是说不理解外键的概念，而是说在高并发场景下用应用层来保证一致性。
- **实际项目中可能怎么用**：① 做毕设时，表设计要先画 ER 图，再按范式检查 ② 面试中问到"为什么不用外键"时，能说出阿里的规范和理由会加分 ③ DROP vs DELETE vs TRUNCATE 是每次面试几乎必问的题。
- **下一步可以学什么**：① 结合 [[MySQL复习笔记]] 中的 SQL 语法把理论转化为实操 ② 用一个实际项目（比如毕设的数据库）来练习表设计 ③ 深入学习索引和事务（高级篇内容）
- **AI给我的学习建议**：这篇笔记非常适合考前快速复习，建议你每隔 2 周重读一遍"十大核心"部分。特别是三大范式和主外键对比，初学者最容易混淆。建议你试着用 ER 图设计一个简单的"学生选课系统"来检验自己是否真正理解了这些概念。