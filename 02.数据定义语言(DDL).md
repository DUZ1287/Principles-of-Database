# 🏗️ 数据定义语言（DDL）| 建库建表从入门到“跑路”
  
> **前置知识**：MySQL 8+ 已装好，能连上命令行或可视化工具  
> **预计阅读时间**：40 分钟（含动手）

---

## 📌 目录

1. [DDL 是啥？一条数据的诞生之路](#1-ddl-是啥一条数据的诞生之路)
2. [SQL 命名规定 & 阿里巴巴规范（血泪教训版）](#2-sql-命名规定--阿里巴巴规范血泪教训版)
3. [库管理：创建、查看、修改、删除（跑路必备）](#3-库管理创建查看修改删除跑路必备)
4. [表管理：创建表（核心中的核心）](#4-表管理创建表核心中的核心)
5. [数据类型详解：选对类型，幸福一生](#5-数据类型详解选对类型幸福一生)
6. [表管理：修改表结构（加列、删列、改名）](#6-表管理修改表结构加列删列改名)
7. [表管理：删除表 & 清空表（手滑就火葬场）](#7-表管理删除表--清空表手滑就火葬场)
8. [综合练习：从零打造员工表](#8-综合练习从零打造员工表)
9. [常见错误列表（新手必看）](#9-常见错误列表新手必看)
10. [总结 & 装逼指南](#10-总结--装逼指南)

---

## 1. DDL 是啥？一条数据的诞生之路

**DDL = Data Definition Language**，数据定义语言。  
它不负责数据本身，只负责 **容器** 的结构 —— 库、表、索引、视图等。

一条完整的数据存储过程分 4 步：

1. **DDL** → 建库、建表（本章）
2. **DML** → 插入数据（下一章）
3. **DQL** → 查询数据（重头戏）
4. **TCL/DCL** → 事务/权限（高阶）

> 类比：  
> DDL 是 **买地皮 + 打地基**  
> DML 是 **往房子里搬家具**  
> DQL 是 **进屋找东西**  
> TCL 是 **装修过程中的后悔药（回滚）**

---

## 2. SQL 命名规定 & 阿里巴巴规范（血泪教训版）

### 2.1 硬性规定（不遵守 MySQL 会报错）

| 规则 | 说明 | 正确例子 | 错误例子 |
|------|------|----------|----------|
| 长度限制 | 库名/表名 ≤30 字符，变量名 ≤29 | `customer_data` | `super_long_customer_info_2026` |
| 允许字符 | A-Z a-z 0-9 _ ，不能数字开头 | `order_1` | `1_order` |
| 禁止空格 | 对象名中间不能有空格 | `user_info` | `user info` |
| 唯一性 | 同库下表不重名，同表下列不重名 | - | - |
| 避免关键字 | 不要用 `SELECT`, `INSERT` 等保留字 | `user_table` | `select`（除非用反引号 \`select\`） |

> 💡 反引号（\`）可以包住关键字当名字，但**别作死**。  
> 官网保留字列表：[MySQL 8.0 Keywords](https://dev.mysql.com/doc/refman/8.0/en/keywords.html)

### 2.2 阿里巴巴规范（不遵守会被同事打死）

> 本规范参考《阿里巴巴 Java 开发手册》中的 MySQL 部分。

- **库名、表名、列名全部小写**，禁止驼峰（比如 `userName` → 不行，要 `user_name`）
- **用下划线分词**：`order_detail`, `alipay_task`
- **命名要有描述性**：`book_price` 比 `bp` 好一万倍
- **库名与程序名对应**：程序叫 `EcommercePlatform` → 库名 `ecommerce_platform`
- **表名格式**：`业务名称_表的作用`，例如 `trade_config`, `user_account`
- **列名格式**：`表实体_属性`，例如 `product_name`, `order_quantity`
- **注释必须写**：`COMMENT '注释内容'`，别让你同事猜你的表是干啥的

> 示例（符合规范）：
```sql
CREATE DATABASE ecommerce_platform;
CREATE TABLE user_account (
    user_id INT COMMENT '用户ID',
    user_name VARCHAR(50) COMMENT '用户名'
);
```

---

## 3. 库管理：创建、查看、修改、删除（跑路必备）

### 3.1 创建数据库（4 种方式）

```sql
-- 方式1：最简，使用默认字符集和排序规则（MySQL 8 默认 utf8mb4）
CREATE DATABASE mydb;

-- 方式2：判断不存在再创建（推荐，避免重复执行报错）
CREATE DATABASE IF NOT EXISTS mydb;

-- 方式3：指定字符集
CREATE DATABASE mydb CHARACTER SET utf8mb4;

-- 方式4：指定字符集 + 排序规则（大小写敏感？不敏感？）
CREATE DATABASE mydb CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_as_cs;
```

**MySQL 8 默认值：**
- 字符集：`utf8mb4`（支持表情包 😂）
- 排序规则：`utf8mb4_0900_ai_ci`（`ai`=重音不敏感，`ci`=大小写不敏感）

> ⚠️ 排序规则 `_cs` 表示大小写敏感（case sensitive），`_ci` 表示不敏感。  
> 敏感与否影响 `WHERE name = 'abc'` 能否匹配到 `'Abc'`。

### 3.2 查看和使用库

```sql
-- 查看所有库
SHOW DATABASES;

-- 查看当前正在使用的库
SELECT DATABASE();

-- 切换 / 选中库（必须先 use 才能操作表）
USE mydb;

-- 查看指定库下的所有表
SHOW TABLES FROM mydb;

-- 查看创建库的完整 SQL 语句（可以看到字符集等）
SHOW CREATE DATABASE mydb;
```

### 3.3 修改库（只能改字符集/排序规则，不能改名！）

```sql
-- 修改字符集
ALTER DATABASE mydb CHARACTER SET gbk;

-- 修改排序规则
ALTER DATABASE mydb COLLATE utf8mb4_0900_as_cs;

-- 同时修改
ALTER DATABASE mydb CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
```

> ⚠️ **数据库不能改名**！那些可视化工具的“重命名”其实是新建库 + 复制所有表 + 删旧库，数据量大时会很慢且可能丢数据。

### 3.4 删除数据库（跑路前必学）

```sql
-- 直接删（如果不存在就报错）
DROP DATABASE mydb;

-- 推荐：判断存在再删
DROP DATABASE IF EXISTS mydb;
```

> 删库前一定要备份！没有备份就删库的，请自觉准备好跑路简历。

### 3.5 实战练习：博客平台数据库

```sql
-- 场景1：创建 blog_platform 库，支持多语言（utf8mb4）
CREATE DATABASE blog_platform CHARACTER SET utf8mb4;

-- 场景2：查看默认字符集和排序规则
SHOW VARIABLES LIKE 'character_set_database';
SHOW VARIABLES LIKE 'collation_database';

-- 场景3：修改排序规则为大小写敏感
ALTER DATABASE blog_platform COLLATE utf8mb4_0900_as_cs;

-- 场景4：确认修改
SHOW CREATE DATABASE blog_platform;

-- 场景5：项目黄了，删库跑路
DROP DATABASE IF EXISTS blog_platform;
```

---

## 4. 表管理：创建表（核心中的核心）

### 4.1 创建表的基本语法（拆解版）

```sql
CREATE TABLE [IF NOT EXISTS] 表名 (
    列名1 数据类型 [列级约束] [COMMENT '注释'],
    列名2 数据类型 [列级约束] [COMMENT '注释'],
    ...
    [表级约束]
) [表选项] [COMMENT '表注释'];
```

**必填项：** 表名、列名、数据类型  
**可选项：** `IF NOT EXISTS`、约束、注释、表选项（引擎、字符集）

> 一个完整的例子（博客文章表）：

```sql
CREATE TABLE IF NOT EXISTS posts (
    post_id INT AUTO_INCREMENT PRIMARY KEY COMMENT '文章ID',
    title VARCHAR(255) NOT NULL COMMENT '标题',
    content TEXT NOT NULL COMMENT '内容',
    author_id INT NOT NULL COMMENT '作者ID',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    CONSTRAINT fk_author FOREIGN KEY (author_id) REFERENCES authors(author_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT '博客文章表';
```

**各要素说明：**

- `AUTO_INCREMENT`：自增列（通常用于主键）
- `PRIMARY KEY`：主键约束（唯一且非空）
- `NOT NULL`：不能为空
- `DEFAULT`：默认值
- `COMMENT`：注释（强烈建议写）
- `CONSTRAINT ... FOREIGN KEY`：外键约束（表级）
- `ENGINE=InnoDB`：存储引擎（InnoDB 支持事务和外键）
- `DEFAULT CHARSET=utf8mb4`：表字符集（可覆盖库的字符集）

### 4.2 小练习：图书管理系统

```sql
-- 场景1：创建数据库 book_libs，使用 utf8mb4，大小写敏感排序
CREATE DATABASE book_libs CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_as_cs;

USE book_libs;

-- 场景2：创建图书表 books
CREATE TABLE IF NOT EXISTS books (
    book_name VARCHAR(20) COMMENT '图书名称',
    book_price DECIMAL(4,1) COMMENT '图书价格（总长4位，小数1位）',  -- 修正：不用 DOUBLE
    book_num INT COMMENT '图书数量'
) CHARSET=utf8mb4 COMMENT '图书表';
```

> 💡 `DECIMAL(4,1)` 表示总长度 4 位，小数点后 1 位，比如 99.9 或 123.4。  
> **对于金额、价格等要求精确的数据，永远使用 `DECIMAL`，不要用 `FLOAT` 或 `DOUBLE`**。

---

## 5. 数据类型详解：选对类型，幸福一生

### 5.1 整数类型

| 类型 | 字节 | 有符号范围 | 无符号范围（`UNSIGNED`） |
|------|------|------------|--------------------------|
| TINYINT | 1 | -128 ~ 127 | 0 ~ 255 |
| SMALLINT | 2 | -32768 ~ 32767 | 0 ~ 65535 |
| MEDIUMINT | 3 | -8388608 ~ 8388607 | 0 ~ 16777215 |
| INT | 4 | -21亿 ~ 21亿 | 0 ~ 42亿 |
| BIGINT | 8 | 巨大 | 巨大 |

**使用建议：**
- 年龄、状态 → `TINYINT`
- 普通 ID → `INT`
- 海量数据 ID → `BIGINT`
- 非负值加上 `UNSIGNED`（如 `age TINYINT UNSIGNED`）

```sql
stu_age TINYINT UNSIGNED COMMENT '年龄，0~255',
```

### 5.2 浮点数 & 定点数

**修正点**：删除“未来可能移除”的不确定描述，明确推荐 DECIMAL。

| 类型 | 字节 | 说明 | 精度 |
|------|------|------|------|
| FLOAT | 4 | 单精度 | 大约7位小数 |
| DOUBLE | 8 | 双精度 | 大约15位小数 |
| DECIMAL(M,D) | 可变 | 定点数，精确 | 由 M,D 决定 |

**注意：**
- 对于**金额、价格、体重、身高**等需要精确计算的数值，**必须使用 `DECIMAL`**。
- `FLOAT` 和 `DOUBLE` 存在精度丢失问题，不适合存储需要比较相等性的数据。

```sql
-- 正确示例
price DECIMAL(10,2) COMMENT '商品价格，精确到分',
height DECIMAL(4,1) COMMENT '身高（米），精确到0.1米',
```

### 5.3 字符串类型（核心）

**修正点**：增加 VARCHAR 最大长度的补充说明。

| 类型 | 最大长度 | 说明 | 适用场景 |
|------|----------|------|----------|
| CHAR(M) | 255 字符 | 定长，M 省略默认为 1 | 固定长度：性别、手机号、MD5 |
| VARCHAR(M) | 16383 字符（utf8mb4下） | 变长，必须指定 M | 名字、地址、标题 |
| TEXT | 65535 字节 ≈ 64KB | 大文本，不占行内 | 文章内容、评论 |
| LONGTEXT | 4GB | 超大文本 | 真·长篇小说 |

**重点知识：**
- `VARCHAR` 最大长度受限于行大小（一行的总字节 ≤ 65535）。utf8mb4 每个字符最多 4 字节，所以理论单列最大字符数为 `(65535 - 1 - 其他列占用的字节) / 4`。在单列且没有其他大字段的简单场景下，约为 **16383 字符**。实际项目中请根据整行所有列的总长度来设计。
- `CHAR` 会右侧补空格，查询时自动去掉；`VARCHAR` 保留尾部空格。
- 大文本（>64KB）建议单独存文件，数据库只存文件路径，否则性能血崩。

### 5.4 日期时间类型

**修正点**：将 `utf8_general_ci` 示例替换为 `utf8mb4_0900_ai_ci`，并添加兼容性说明。

| 类型 | 格式 | 范围 | 说明 |
|------|------|------|------|
| DATE | `YYYY-MM-DD` | 1000-01-01 ~ 9999-12-31 | 只有日期 |
| TIME | `HH:MM:SS` | -838:59:59 ~ 838:59:59 | 时间或时长 |
| YEAR | `YYYY` | 1901 ~ 2155 | 年份（建议用4位） |
| DATETIME | `YYYY-MM-DD HH:MM:SS` | 1000~9999 | 日期+时间，占8字节 |
| TIMESTAMP | `YYYY-MM-DD HH:MM:SS` | 1970~2038 | 带时区，自动更新，占4字节 |

**自动初始化和更新（常用）：**
```sql
-- 插入时自动设为当前时间，更新时也自动更新
created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
-- 仅插入时自动设当前时间
updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
```

> 💡 **字符集与排序规则说明**：  
> - MySQL 8.0 默认字符集为 `utf8mb4`，默认排序规则为 `utf8mb4_0900_ai_ci`（`ai`=重音不敏感，`ci`=大小写不敏感）。  
> - 旧版 MySQL 5.7 常用 `utf8_general_ci`，但 `utf8` 不是真正的 UTF-8（最多3字节，存不了表情）。**强烈建议使用 `utf8mb4` 系列**。  
> - 如果你的应用需要大小写敏感，使用 `_cs` 结尾的排序规则，如 `utf8mb4_0900_as_cs`。

### 5.5 数据类型选择原则

1. **尽量选小范围**：`TINYINT` 能存下的别用 `INT`
2. **金额必须用 `DECIMAL`**，不用 `FLOAT`/`DOUBLE`（精度会丢失）
3. **字符串固定长度用 `CHAR`**，可变长度用 `VARCHAR`
4. **不要用 `TEXT` 存大文本**（除非你真的需要）
5. **时间类型优先 `DATETIME`**（范围广），除非你需要自动更新和时区特性

---

## 6. 表管理：修改表结构（加列、删列、改名）

### 6.1 修改列（字段）

```sql
-- 1. 添加列（可指定位置 FIRST 或 AFTER 某列）
ALTER TABLE 表名 ADD 列名 数据类型 [约束] [FIRST|AFTER 已有列];

-- 2. 修改列名（同时可以改类型和位置）
ALTER TABLE 表名 CHANGE 原列名 新列名 新数据类型 [FIRST|AFTER ...];

-- 3. 修改列的类型或约束（不改名）
ALTER TABLE 表名 MODIFY 列名 新数据类型 [FIRST|AFTER ...];

-- 4. 删除列
ALTER TABLE 表名 DROP 列名;
```

**示例：**
```sql
-- 在 employees 表中，将 mobile 字段移到 code 字段后面
ALTER TABLE employees MODIFY mobile VARCHAR(20) AFTER code;

-- 将 birth 字段改名为 birthday，且改为 DATE 类型
ALTER TABLE employees CHANGE birth birthday DATE;

-- 修改 sex 字段类型为 CHAR(1)
ALTER TABLE employees MODIFY sex CHAR(1);

-- 删除 note 字段
ALTER TABLE employees DROP note;

-- 增加 favoriate_activity 字段，类型 VARCHAR(100)
ALTER TABLE employees ADD favoriate_activity VARCHAR(100);
```

### 6.2 修改表名

```sql
-- 两种写法都可以
ALTER TABLE 旧表名 RENAME TO 新表名;
ALTER TABLE 旧表名 RENAME 新表名;
-- 例：
ALTER TABLE employees RENAME TO employees_info;
```

---

## 7. 表管理：删除表 & 清空表（手滑就火葬场）

### 7.1 删除表（结构 + 数据都没了）

```sql
-- 删一张表
DROP TABLE IF EXISTS 表名;

-- 删多张表（用逗号分隔）
DROP TABLE IF EXISTS 表1, 表2, 表3;
```

### 7.2 清空表数据（保留结构，数据全丢）

```sql
TRUNCATE TABLE 表名;
```

**`TRUNCATE` vs `DELETE` 区别：**

| 操作 | 是否可回滚 | 速度 | 重置自增列 | 触发触发器 |
|------|------------|------|-------------|-------------|
| `TRUNCATE` | ❌ 不可回滚 | 极快 | ✅ 重置为 1 | ❌ |
| `DELETE` | ✅ 可回滚 | 慢 | ❌ 不重置 | ✅ |

> ⚠️ **`TRUNCATE` 和 `DROP` 都是“核武器”级别操作，执行前请深呼吸三秒。**  
> 一旦执行，没有后悔药（除非你有备份）。

---

## 8. 综合练习：从零打造员工表

**要求：**  
1. 创建表格 `employees`  
2. 将 `mobile` 字段修改到 `code` 字段后面  
3. 将 `birth` 字段改名为 `birthday`  
4. 修改 `sex` 字段类型为 `CHAR(1)`  
5. 删除 `note` 字段  
6. 增加字段 `favoriate_activity`，类型 `VARCHAR(100)`  
7. 将表名改为 `employees_info`

**参考实现（逐步执行）：**

```sql
-- 1. 创建表（假设已有数据库）
USE company_db;
CREATE TABLE employees (
    id INT PRIMARY KEY AUTO_INCREMENT,
    code VARCHAR(20),
    mobile VARCHAR(20),
    name VARCHAR(50),
    sex VARCHAR(10),
    birth DATE,
    note TEXT
);

-- 2. 移动 mobile 字段到 code 之后
ALTER TABLE employees MODIFY mobile VARCHAR(20) AFTER code;

-- 3. 改 birth → birthday
ALTER TABLE employees CHANGE birth birthday DATE;

-- 4. 修改 sex 类型为 CHAR(1)
ALTER TABLE employees MODIFY sex CHAR(1);

-- 5. 删除 note
ALTER TABLE employees DROP note;

-- 6. 添加 favoriate_activity
ALTER TABLE employees ADD favoriate_activity VARCHAR(100);

-- 7. 改表名
ALTER TABLE employees RENAME TO employees_info;
```

---

## 9. 常见错误列表（新手必看）

> 汇总了新手（和老手）最常踩的坑，建议贴屏幕上每天看一遍。

| 错误类型 | 错误示例 | 正确做法 |
|----------|----------|----------|
| 忘记写 WHERE | `UPDATE students SET age = 18;` | 先 `SELECT` 确认，再加 `WHERE` |
| 字符串/日期不加引号 | `INSERT INTO t (name) VALUES (张三);` | 加单引号：`'张三'` |
| 关键字做列名 | `CREATE TABLE t (select INT);` | 要么避免，要么加反引号：\`select\` |
| 列数与值数不匹配 | `INSERT INTO t (a,b) VALUES (1);` | 确保值个数与列名个数一致 |
| 用 DOUBLE 存金额 | `price DOUBLE(10,2)` | 用 `DECIMAL(10,2)` |
| 生产环境直接删表 | `DROP TABLE important;` | 先备份，再三确认，再用 `DROP` |
| 字符集用 utf8（非 mb4） | `CHARACTER SET utf8` | 用 `utf8mb4` 支持表情和更多字符 |
| 排序规则用错导致大小写问题 | `... COLLATE utf8mb4_0900_ai_ci` 却不区分大小写 | 根据业务选择 `_ci` 或 `_cs` |

---

## 10. 总结 & 装逼指南

### 你必须记住的 DDL 核心命令

| 操作 | 命令模板 |
|------|----------|
| 建库 | `CREATE DATABASE [IF NOT EXISTS] 库名;` |
| 删库 | `DROP DATABASE [IF EXISTS] 库名;` |
| 选库 | `USE 库名;` |
| 建表 | `CREATE TABLE [IF NOT EXISTS] 表名 (列定义);` |
| 改表（加列） | `ALTER TABLE 表名 ADD 列名 类型;` |
| 改表（改列名） | `ALTER TABLE 表名 CHANGE 旧名 新名 类型;` |
| 改表（删列） | `ALTER TABLE 表名 DROP 列名;` |
| 删表 | `DROP TABLE [IF EXISTS] 表名;` |
| 清空表 | `TRUNCATE TABLE 表名;` |

### 命名规范速记口诀

> 库表字段都小写，下划线来分单词。  
> 不写注释被人骂，保留关键字别用。  
> 业务名在前表在后，实体属性列名清。

### 数据类型选型口诀

> 整数用 INT 或 TINY，金额必须 DECIMAL。  
> 变长用 VARCHAR，定长用 CHAR 省空间。  
> 时间选 DATETIME，除非你要自动改。  
> 大文本别塞库里，存个路径就完事。

---

**最后一句忠告：**  
DDL 写好了，后面 CRUD 才舒心。DDL 写烂了，后面每天都是“改表导致锁表”、“字段类型装不下数据”、“乱码满天飞”的修罗场。  
**善待未来的自己，从写好每一个 DDL 开始。** 🧘

**下一章预告：** DML 增删改 + DQL 花式查询 —— 终于可以往表里塞数据了！

---

> 学习 SQL 最好的方法不是背，而是动手敲！现在就去命令行里建几个库表试试吧～

