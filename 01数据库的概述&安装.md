## 📌 一、数据库到底是个啥？

**官方定义：**  
数据库是“按照数据结构来组织、存储和管理数据的仓库”。  
**人话翻译：**  
就是你的电脑里专门用来存数据的一块区域，而且存得很有规矩，不像你的“下载”文件夹那样乱七八糟。

### ✅ 数据库的四大核心特点（记住，面试要考）

| 特点 | 解释 | 吐槽 |
|------|------|------|
| 长期存储 | 数据不会自动消失，除非你删它 | 比前任的聊天记录还持久 |
| 有组织 | 数据按固定结构存放，不是乱塞 | 像宜家家具，有图纸 |
| 可共享 | 多个程序/用户能同时访问 | 像共享充电宝，但不容易坏 |
| 统一管理 | 由 DBMS 软件统一调度 | 你不是管理员，你只是普通用户 |

> 💡 数据库本质上就是**磁盘上的文件**，只不过它是“有组织、可共享、长期存储”的高级文件。

---

## 📂 二、数据库的分类：关系型 vs 非关系型

### 🧩 关系型数据库（SQL）

- **结构**：表格形式（行+列）  
- **优点**：支持复杂查询、ACID 事务、数据一致性  
- **缺点**：扩展性一般，不适合超高并发  
- **代表**：MySQL、Oracle、SQLite、PostgreSQL

### 🧩 非关系型数据库（NoSQL）

- **结构**：键值对、文档型（JSON）、列族、图  
- **优点**：灵活、高性能、易扩展  
- **缺点**：事务弱，查询能力有限  
- **代表**：Redis、MongoDB、Cassandra

### 🤔 开发中怎么选？

- 主体业务数据（订单、用户）→ **关系型**
- 缓存、高并发、日志 → **非关系型**
- 大部分项目 = **混合使用**（就像麻辣烫里既加方便面又加青菜）

---

## 🧱 三、关系型数据库的存储模型（E-R 模型）

- **E（Entity）**：实体 → 对应一张表（如：`student` 表）
- **R（Relationship）**：关系 → 表之间可以关联（如：学生 ↔ 成绩）

### 📦 存储层级结构

```
数据库（Database）         ← 最大的容器
 └── 表（Table）           ← 一类实体
      ├── 列（Column）     ← 字段，比如 name、age
      └── 行（Row）        ← 一条记录，比如 "张三", 20
```

> 像极了你整理衣柜：  
> 数据库 = 整个衣柜  
> 表 = 每一层（上衣层 / 裤子层）  
> 列 = 类别（颜色 / 尺码）  
> 行 = 具体某件衣服（红色 S 码 T恤）

---

## 🛠️ 四、数据库管理系统（DBMS）是啥？

> DBMS 就是管理数据库的软件，类似于仓库管理员，你指挥它干活。

常见的关系型 DBMS 大盘点：

| 名称 | 特点 | 适用场景 | 费用 |
|------|------|----------|------|
| Oracle | 业界标杆，功能最强 | 大型企业、银行 | 💰💰💰 |
| MySQL | 性能好，开源免费 | 中小项目、互联网 | 免费 |
| DB2 | IBM 出品，稳如老狗 | 金融、保险 | 💰💰 |
| SQLite | 嵌入式，零配置 | 手机、IoT | 免费 |

📊 **市场使用率排名**（参考 [DB-Engines](https://db-engines.com/en/ranking)）：  
1. Oracle  
2. MySQL  
3. SQL Server  
4. PostgreSQL  
5. MongoDB（非关系型）

> 小贴士：MySQL 虽然被 Oracle 收购，但依然是开源免费的主流选择，别慌。

---

## 💽 五、MySQL 安装（保姆级提醒）

### ✅ 版本选择：**8.0+**，不要再用 5.7！

- 5.7 已于 2023 年 10 月结束“扩展支持”，现在是“末期维护”，官方建议迁移到 8.0。
- 8.0 性能更好、安全性更强、新特性更多。

### 📥 下载地址

- 官网：https://dev.mysql.com/downloads/mysql/
- 选择 **GA（General Availability）** 版本，即稳定正式版。

### 🔧 安装后配置（重要！）

找到 MySQL 安装目录下的 `my.ini` 文件（Windows）或 `my.cnf`（macOS/Linux），添加/修改以下内容：

```ini
[mysqld]
default-storage-engine=INNODB
character-set-server=utf8
collation-server=utf8_general_ci

[mysql]
default-character-set=utf8
```

> 不配置编码？那你等着看乱码吧，像收到了外星人的电报 👽

---

## 📝 六、SQL 是什么？别怕，它只是一种语言

**SQL = Structured Query Language**  
用来和关系型数据库沟通的专用语言，就像你会用英语点餐一样。

### 🧭 SQL 分类大全（按重要程度排序）

| 分类 | 全称 | 作用 | 学习优先级 |
|------|------|------|-------------|
| DQL | Data Query Language | 查询数据（`SELECT`） | ⭐⭐⭐⭐⭐ |
| DML | Data Manipulation Language | 增删改数据（`INSERT`, `UPDATE`, `DELETE`） | ⭐⭐⭐⭐ |
| DDL | Data Definition Language | 定义表结构（`CREATE`, `ALTER`, `DROP`） | ⭐⭐⭐ |
| TCL | Transaction Control Language | 事务控制（`COMMIT`, `ROLLBACK`） | ⭐⭐ |
| DCL | Data Control Language | 权限管理（`GRANT`, `REVOKE`） | ⭐ |

> 📌 学习顺序建议：**DDL → DML → DQL → TCL → DCL**  
> DQL 是重中之重，面试 90% 的问题都在这。

---

## 💻 七、命令行操作入门（装逼必备）

### 🔌 连接 MySQL

```bash
mysql -u root -p -h localhost
```

- `-u`：用户名（root 是超级管理员）
- `-p`：密码（直接跟密码没空格，或者只写 `-p` 然后回车输入）
- `-h`：主机地址（本地用 localhost）

### 📌 基础命令速查

```sql
-- 查看当前版本
select version();

-- 查看所有数据库
show databases;

-- 使用某个数据库
use 数据库名;

-- 查看当前库的所有表
show tables;

-- 退出连接
exit;
```

### 📝 SQL 注释写法

```sql
# 单行注释（这种最常用）

-- 单行注释（注意 -- 后面必须有空格）

/*
  多行注释
  可以写很长
*/
```

> 注释是写给你自己或队友看的，不然三天后你看着自己的 SQL 会怀疑人生。

---

## 🖥️ 八、可视化工具推荐（告别小黑窗）

| 工具 | 优点 | 缺点 | 推荐人群 |
|------|------|------|----------|
| MySQL Workbench | 官方免费，功能全 | 全英文界面 | 英语较好的同学 |
| Navicat | 强大，支持多种数据库 | 免费版功能阉割 | 愿意掏钱的专业用户 |
| SQLyog | 界面简洁，适合新手 | 免费版无代码提示 | 初学者（Windows） |
| DBeaver | 开源免费，跨平台 | 稍重，配置略复杂 | 喜欢折腾的开源爱好者 |

> 💡 个人推荐：Windows 用户先用 **SQLyog Community**，Mac 用户用 **Navicat 试用版**（14 天足够你学完基础了）。  
> 工具只是辅助，核心还是 SQL 语句本身。

---

## 🧠 九、小结（面试前背一遍）

1. **数据库** = 有组织、长期存储、可共享的数据集合
2. **关系型** = 表格 + 关系，**非关系型** = 灵活 + 高性能
3. **MySQL 8+** 是当前主流，5.7 已过时
4. **SQL** 分为 DDL、DML、DQL、TCL、DCL，重点学 DQL
5. 命令行连接：`mysql -u root -p`
6. 可视化工具随便选，会 SQL 才是硬道理

---

## 🎉 十、最后的最后

> 数据库就像你人生的备忘录，SQL 就是你跟它交流的语言。  
> 学会它，你不仅能存数据，还能存住未来的饭碗 🍚  
> 加油，未来的 DBA 或后端大佬！
---
