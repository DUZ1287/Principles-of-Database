# 🗄️ 数据库概述 & MySQL 安装 | 超详细·幽默·大学生笔记


## 📌 目录

1. [数据库到底是个啥？](#1-数据库到底是个啥)
2. [数据库的分类：关系型 vs 非关系型](#2-数据库的分类关系型-vs-非关系型)
3. [关系型数据库的存储模型（E-R 模型）](#3-关系型数据库的存储模型e-r-模型)
4. [数据库管理系统（DBMS）大乱斗](#4-数据库管理系统dbms大乱斗)
5. [MySQL 安装：从下载到跑起来](#5-mysql-安装从下载到跑起来)
6. [SQL 是什么？怎么学？](#6-sql-是什么怎么学)
7. [可视化工具：告别小黑窗](#7-可视化工具告别小黑窗)
8. [总结 & 课后装逼指南](#8-总结--课后装逼指南)

---

## 1. 数据库到底是个啥？

### 1.1 程序中的数据存在哪？

你写了一个学生管理系统，程序运行时数据在内存里，一断电就没了。  
你想存下来，下次开机还能看到 → **必须存到硬盘**。

最简单的方案：**存文件**，比如 `students.txt`。

```txt
张三,18,男
李四,19,女
```

但问题来了：

- 你要查“所有 18 岁的男生” → 得自己写代码遍历文件、解析字符串 → 麻烦
- 你要改李四的年龄 → 得找到那一行，修改，再写回文件 → 容易出错
- 多个程序同时改同一个文件 → 数据错乱

**于是数据库诞生了** —— 它帮你搞定这些脏活累活。

### 1.2 数据库的官方定义（背下来面试用）

> 数据库是 **长期存储** 在计算机内的、**有组织的**、**可共享的**、**统一管理** 的大量数据的集合。

用人话翻译：

| 特性 | 解释 | 生动例子 |
|------|------|----------|
| 长期存储 | 数据不会自动消失 | 比你前任的聊天记录还持久 |
| 有组织的 | 按照固定结构存放（如表格） | 像宜家家具，有图纸，不是乱堆 |
| 可共享的 | 多个用户/程序可以同时访问 | 像共享充电宝，但不会坏 |
| 统一管理 | 有专门的软件（DBMS）负责 | 你不是仓库管理员，你只是普通顾客 |

### 1.3 数据库本质上是什么？

> 数据库在磁盘上就是 **文件**，只不过是一种特殊格式的文件，由数据库管理系统（DBMS）去读写。

- 你用记事本打开数据库文件，看到的是乱码 —— 正常，因为 DBMS 用自己的编码方式组织数据。
- 类比：你看到的 `.docx` 文件也是 zip 压缩包，但你知道用 Word 打开。

---

## 2. 数据库的分类：关系型 vs 非关系型

数据库主要分成两大阵营：**关系型（SQL）** 和 **非关系型（NoSQL）**。

### 2.1 关系型数据库（SQL）

- **数据模型**：二维表格（行+列），表之间可以建立关系（主键、外键）。
- **代表产品**：MySQL、Oracle、PostgreSQL、SQLite、SQL Server。
- **核心优势**：
  - ✅ 支持复杂的 SQL 查询（多表 JOIN、子查询、聚合）
  - ✅ 支持 **ACID 事务**（原子性、一致性、隔离性、持久性）—— 保证数据不出错
  - ✅ 数据完整性约束（比如身份证号不能重复、不能为空）
- **劣势**：
  - ❌ 扩展性相对差（一般只能垂直扩展 → 加内存、换更强 CPU）
  - ❌ 高并发写入性能不如某些 NoSQL

### 2.2 非关系型数据库（NoSQL）

- **数据模型**：灵活，常见有：
  - 键值型（Redis）
  - 文档型（MongoDB，存 JSON）
  - 列族型（Cassandra，HBase）
  - 图形数据库（Neo4j，存节点和关系）
- **核心优势**：
  - ✅ 高性能读写（尤其适合缓存、实时计数）
  - ✅ 天然支持分布式，水平扩展方便
  - ✅ 无固定表结构，适合字段经常变化的场景
- **劣势**：
  - ❌ 事务支持弱（很多只保证最终一致性）
  - ❌ 复杂查询能力有限（比如多表关联查询）

### 2.3 实际项目中怎么选？

> 记住这个原则：**主从搭配，干活不累**。

- 主体业务数据（用户信息、订单、商品） → **关系型数据库（主）**
- 缓存数据（Session、热点数据） → **非关系型（如 Redis）**
- 高并发写入（点赞数、访问量） → **非关系型**
- 日志、半结构化数据 → **文档型 NoSQL（如 MongoDB）**

绝大部分互联网项目都是 **混合使用**，就像麻辣烫里既加方便面又加青菜，各取所长。

---

## 3. 关系型数据库的存储模型（E-R 模型）

关系型数据库的设计遵循 **E-R 模型**（Entity-Relationship）。

### 3.1 E 和 R 是啥？

- **Entity（实体）**：现实世界中一类事物 → 对应数据库中的 **一张表**  
  例如：学生、课程、员工、部门
- **Relationship（关系）**：实体之间的联系 → 表之间的 **关联**  
  例如：学生选了课程（多对多）、员工属于部门（多对一）

### 3.2 存储单位层级（从大到小）

```
数据库（Database）           # 一个项目通常用一个库
 └── 表（Table）             # 一类实体，比如 student
      ├── 列（Column）       # 字段，比如 id, name, age
      └── 行（Row）          # 一条具体记录，比如 (1, '张三', 20)
```

**类比理解：**

- 数据库 = 一个 Excel 文件
- 表 = 文件里的一个 Sheet（工作表）
- 列 = 表头（姓名、年龄、性别）
- 行 = 每一行的数据

### 3.3 几个重要概念（提前眼熟）

- **主键（Primary Key）**：唯一标识一行记录的列，比如 `student_id`，不能重复，不能为空。
- **外键（Foreign Key）**：指向另一张表主键的列，用来建立表间关系。
- **索引（Index）**：帮你快速查找数据的“目录”，提高查询速度。

> 这些东西后面会详细讲，现在你只需要知道：关系型数据库是 **有规矩** 的。

---

## 4. 数据库管理系统（DBMS）大乱斗

### 4.1 什么是 DBMS？

> DBMS = 数据库管理系统，就是一款 **管理数据库的软件**。  
> 用户通过它来操作数据库（增删改查），而不是直接操作文件。

类比：  
- 数据库 = 仓库  
- DBMS = 仓库管理员 + 管理系统（你喊一声，它帮你搬货）

### 4.2 常见关系型 DBMS 对比

| 名称 | 开发者 | 特点 | 适用场景 | 价格 |
|------|--------|------|----------|------|
| Oracle | Oracle 公司 | 功能最强大，安全性高，支持所有工业标准 | 银行、电信、大型企业 | 💰💰💰 很贵 |
| MySQL | 原 MySQL AB，现 Oracle | 性能好，开源免费，生态丰富 | 互联网公司、中小企业 | 免费（社区版） |
| DB2 | IBM | 多进程多线程，稳定可靠 | 金融、保险、大型国企 | 💰💰 贵 |
| SQLite | D. Richard Hipp | 嵌入式，零配置，轻量级 | 手机 App、智能家居、嵌入式设备 | 免费 |
| PostgreSQL | 开源社区 | 功能接近 Oracle，扩展性强 | 需要高级 SQL 特性的场景 | 免费 |

### 4.3 市场使用率排名（参考 DB-Engines）

截至 2026 年 5 月趋势：

1. Oracle（常年第一，但份额被 MySQL 和 PG 蚕食）
2. MySQL（最流行的开源关系型数据库）
3. SQL Server（微软）
4. PostgreSQL（上升势头很猛）
5. MongoDB（非关系型中的扛把子）

> 💡 **结论**：MySQL 依然是中小项目和新手的首选，社区资料多，出了问题好百度。

### 4.4 MySQL 的版本怎么选？

- **MySQL 5.7**：曾经很经典，但 Oracle 在 2023 年 10 月终止了“扩展支持”，现在只提供“Sustaining Support”（也就是快断奶了）。
- **MySQL 8.0**：性能更好、安全性更强、支持窗口函数、CTE 等现代 SQL 特性。官方强烈建议迁移到 8.0。

> **一句话：直接学 MySQL 8.0+，别在 5.7 上浪费时间。**

---

## 5. MySQL 安装：从下载到跑起来

### 5.1 下载地址

官网：https://dev.mysql.com/downloads/mysql/  

选择对应的操作系统（Windows / macOS / Linux），然后选择 **GA（General Availability）** 版本，也就是稳定正式版。

> 不要下载 Development Release（开发版），那是给勇士们踩坑用的。

### 5.2 Windows 安装步骤（精华版）

1. 双击安装包，选择 **Custom**（自定义），这样可以只安装 MySQL Server 和 Workbench（可选）。
2. 一路 Next，到 **Type and Networking**：
   - Config Type 选择 **Development Computer**（开发机，占用资源较少）
   - Port 默认 3306（不要改，除非冲突）
3. 设置 root 密码（一定要记住！忘了会很麻烦）
4. 设置 Windows 服务名（默认 MySQL80），建议勾选 **Start the MySQL Server at System Startup**（开机自启）
5. 执行安装，Finish。

### 5.3 核心配置文件 `my.ini`（重要）

MySQL 安装目录下（默认 `C:\ProgramData\MySQL\MySQL Server 8.0\my.ini` 或 `C:\Program Files\MySQL\MySQL Server 8.0\my.ini`），找到并修改：

```ini
[mysqld]
# 默认存储引擎（InnoDB 支持事务，必须的）
default-storage-engine=INNODB

# 服务器编码设置为 utf8（支持中文）
character-set-server=utf8
collation-server=utf8_general_ci

# 允许最大连接数（根据自己电脑配置调整，开发机 200 够用）
max_connections=200

[mysql]
# 客户端默认编码
default-character-set=utf8

[client]
# 连接默认编码
default-character-set=utf8
```

**保存后重启 MySQL 服务**：  
`Win + R` → `services.msc` → 找到 `MySQL80` → 右键重启。

### 5.4 常见安装问题（避坑指南）

| 问题现象 | 可能原因 | 解决办法 |
|----------|----------|----------|
| 安装最后一步卡住 | 防火墙阻止 | 暂时关闭防火墙，安装完再开 |
| 无法启动服务 | my.ini 配置错误 | 检查路径是否包含中文、编码配置是否正确 |
| 忘记 root 密码 | 人傻了 | 网上搜 “MySQL 8.0 重置密码”，有教程，但很麻烦 |
| 端口 3306 被占用 | 之前装过 MySQL 或其它软件 | 改端口（比如 3307），或者卸载干净重装 |

### 5.5 macOS 安装

推荐用 **Homebrew** 安装（没装 brew 的先去装）：

```bash
brew install mysql
# 启动服务
brew services start mysql
# 设置 root 密码（首次登录无密码）
mysql_secure_installation
```

或者直接下载 `.dmg` 安装包，双击安装，步骤类似 Windows。

> Mac 用户注意：配置文件位置在 `/usr/local/etc/my.cnf`，没有就自己新建。

### 5.6 测试是否安装成功

打开命令行（cmd 或 terminal），输入：

```bash
mysql -u root -p
```

输入密码，如果出现 `mysql>` 提示符，说明成功。然后执行：

```sql
select version();
+-----------+
| version() |
+-----------+
| 8.0.36    |
+-----------+
1 row in set (0.00 sec)
```

看到版本号，完美！🎉

---

## 6. SQL 是什么？怎么学？

### 6.1 定义

**SQL** = Structured Query Language，结构化查询语言。  
它是关系型数据库的通用语言，所有主流关系型数据库都支持 SQL（尽管有些方言差异）。

> 学 SQL 就像学英语，学会了你能跟所有关系型数据库交流。

### 6.2 SQL 分类（务必记牢）

| 分类 | 全称 | 作用 | 关键字示例 | 学习优先级 |
|------|------|------|-------------|-------------|
| **DDL** | Data Definition Language | 定义数据库结构（库、表、列） | `CREATE`, `ALTER`, `DROP` | ⭐⭐⭐ |
| **DML** | Data Manipulation Language | 操作数据（增、删、改） | `INSERT`, `UPDATE`, `DELETE` | ⭐⭐⭐⭐ |
| **DQL** | Data Query Language | 查询数据（最最最重要） | `SELECT` | ⭐⭐⭐⭐⭐ |
| **TCL** | Transaction Control Language | 事务控制 | `COMMIT`, `ROLLBACK`, `SAVEPOINT` | ⭐⭐ |
| **DCL** | Data Control Language | 权限管理 | `GRANT`, `REVOKE` | ⭐ |

> 学习路线：先 DDL 建表，再 DML 塞数据，然后疯狂练习 DQL 查询（面试全考这个），最后了解 TCL 和 DCL。

### 6.3 第一个 SQL 命令

连接 MySQL 后，你可以试试这些：

```sql
-- 查看所有数据库
show databases;

-- 创建一个新数据库（名字叫 mydb）
create database mydb;

-- 进入这个数据库
use mydb;

-- 查看当前数据库里的表
show tables;
```

### 6.4 注释写法（养成好习惯）

```sql
# 这是单行注释（推荐，简洁）

-- 这也是单行注释（注意 -- 后面有一个空格）

/*
  多行注释
  可以写很多行
  用于解释复杂逻辑
*/
```

> 注释是写给未来的自己看的，别嫌麻烦。不信你一个月后再来看自己不注释的 SQL，保证一脸懵逼。

### 6.5 退出 MySQL

```sql
exit;
-- 或者
quit;
```

---

## 7. 可视化工具：告别小黑窗

命令行虽然看起来专业，但效率低，没有语法高亮和提示。我们换图形化工具。

### 7.1 常见工具对比

| 工具 | 平台 | 优点 | 缺点 | 推荐指数 |
|------|------|------|------|----------|
| **MySQL Workbench** | Win/Mac/Linux | 官方免费，功能全面 | 全英文，界面略老 | ⭐⭐⭐⭐ |
| **Navicat** | Win/Mac | 强大，支持多种数据库，界面好看 | 收费，免费版阉割严重 | ⭐⭐⭐ |
| **SQLyog** | Win | 简洁，适合新手 | 免费版无代码提示 | ⭐⭐⭐ |
| **DBeaver** | Win/Mac/Linux | 开源免费，支持几乎所有数据库 | 较重，配置稍复杂 | ⭐⭐⭐⭐ |
| **DataGrip** | 全平台 | JetBrains 出品，强大到爆炸 | 收费（学生可免费申请） | ⭐⭐⭐⭐⭐ |

### 7.2 新手推荐方案

- **Windows 用户**：先用 **SQLyog Community**（免费，简单）  
  下载：资料/软件/window/SQLyog-13.1.7-0.x64Community.exe  
  安装后新建连接：  
  - Host: `localhost`  
  - User: `root`  
  - Password: 你的密码  
  - Port: `3306`

- **Mac 用户**：用 **DBeaver**（开源免费）或 **Navicat Premium 试用版**（14 天）

### 7.3 连接失败的常见原因

1. MySQL 服务没启动 → 去服务里启动（Windows）或 `brew services start mysql`（Mac）
2. 密码错误 → 重设密码
3. 端口改了但工具里还是 3306 → 改成你设置的那个端口
4. 防火墙拦截 → 添加 MySQL 端口到允许列表

---

## 8. 总结 & 课后装逼指南

### 📖 你需要记住的核心知识点

1. **数据库** = 长期存储 + 有组织 + 可共享 + 统一管理
2. **关系型** = 表格 + 约束 + 事务；**非关系型** = 灵活 + 高性能 + 易扩展
3. **E-R 模型**：实体（表）和关系（表间关联）
4. **DBMS 选型**：新手学 MySQL 8.0
5. **MySQL 安装要点**：选 GA 版，配置 my.ini 编码，记好 root 密码
6. **SQL 分类**：DDL, DML, DQL, TCL, DCL，重点学 DQL
7. **可视化工具**：SQLyog（Win）或 DBeaver（Mac）够用了

### 🎯 动手任务（不做等于白看）

- [ ] 在自己的电脑上成功安装 MySQL 8.0
- [ ] 配置好 my.ini，重启服务，确认编码为 utf8
- [ ] 用命令行连接，执行 `show databases;`
- [ ] 安装一个可视化工具，成功连接到 MySQL
- [ ] 创建一个数据库 `test_db`，然后在命令行和工具里分别看看

### 💡 最后的最后

> 数据库是你未来后端开发的基石，SQL 是你吃饭的筷子。  
> 第一部分的笔记到这里结束，下一部分我们将真正开始 **建表、插数据、写各种花式查询**。  
> 如果这份笔记让你觉得数据库没那么可怕，甚至有点意思，那就给个 ⭐Star 鼓励一下叭！


**Part 1 完结，撒花 ✿✿ヽ(°▽°)ノ✿**
